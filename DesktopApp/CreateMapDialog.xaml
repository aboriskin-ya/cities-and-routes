<Window x:Class="DesktopApp.CreateMapDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopApp"
        mc:Ignorable="d"
        Title="CreateMapDialog" Height="450" Width="450">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.8*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <DockPanel LastChildFill="True">
            <Label DockPanel.Dock="Left" VerticalAlignment="Center" Target="{Binding ElementName=mapName}">Map name:</Label>
            <TextBox VerticalAlignment="Center" Name="mapName"/>
        </DockPanel>

        <Border Grid.Row="1" Grid.ColumnSpan="3" BorderBrush="Gray" BorderThickness="2" Margin="0 10 0 10">
        </Border>

        <Button Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Left">Create</Button>
    </Grid>
</Window>
