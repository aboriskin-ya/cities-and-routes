<Window x:Class="DesktopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DesktopApp.ViewModels"
        xmlns:uc="clr-namespace:DesktopApp.UserControllers"
        xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top" 
            Height="25" 
            VerticalAlignment="Top">
            <MenuItem 
                Header="Map">
                <MenuItem 
                    Header="Create new" 
                    Command="{Binding ShowCreateMapDialogCommand}">
                </MenuItem>
                <MenuItem 
                    Header="Select existing" >
                </MenuItem>
            </MenuItem>
        </Menu>
        <DockPanel DockPanel.Dock="Right" Width="130">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding ImageHeight}" Grid.Row="0" Grid.Column="0"/>
                <TextBox Text="{Binding ImageWidth}" Grid.Row="0" Grid.Column="1"/>
                <TextBox Text="{Binding Offset.Top,StringFormat='top:{0}'}" Grid.Row="1" Grid.Column="0"/>
                <TextBox Text="{Binding Offset.Right,StringFormat='right:{0}'}" Grid.Row="1" Grid.Column="1"/>
                <TextBox Text="{Binding Offset.Bottom,StringFormat='bottom:{0}'}" Grid.Row="2" Grid.Column="0"/>
                <TextBox Text="{Binding Offset.Left,StringFormat='left:{0}'}" Grid.Row="2" Grid.Column="1"/>
                <TextBox Text="{Binding OffsetValue.X,StringFormat='x:{0}'}" Grid.Row="3" Grid.Column="0"/>
                <TextBox Text="{Binding OffsetValue.Y,StringFormat='y:{0}'}" Grid.Row="3" Grid.Column="1"/>
            </Grid>
        </DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <uc:MapControl OffsetValue="{Binding OffsetValue, Mode=TwoWay}"
                           ScaleValue="{Binding ScaleValue,Mode=TwoWay}"
                           MapImageSource="{Binding MapImageSource}"
                           TransformPosition="{Binding TransformPosition,Mode=TwoWay}"
                           ImageHeight="{Binding ImageHeight, Mode=TwoWay}"
                           ImageWidth="{Binding ImageWidth,Mode=TwoWay}"
                           Offset="{Binding Offset, Mode=TwoWay}"
                           PPW="{Binding PPW, Mode=TwoWay}"
                           PPH="{Binding PPH, Mode=TwoWay}"
                           />
        </DockPanel>
    </DockPanel>
</Window>
